# 第三阶段：可靠路由、故障切换与 API 接入

## A. 客户端 API 与密钥管理

- [x] 前端显示星枢 OpenAI-compatible Base URL、`/v1/models`、`/v1/chat/completions` 与请求示例。
- [x] 管理员可在前端生成独立客户端 API Key；完整 Key 仅创建时显示一次并支持复制。
- [x] 服务端只持久化 Key 哈希和前缀，不保存、返回或记录完整明文。
- [x] Key 支持名称、作用域、创建时间、过期时间、启停、轮换、撤销和最近使用时间。
- [x] `/v1/*` 使用客户端 Key 鉴权；管理员 Key 与客户端 Key 完全隔离。
- [x] 提供可审计、可回滚的兼容迁移模式，升级时不突然中断现有调用。

## B. 可靠路由

- [x] 只从已完成两级批准、Provider 可用且能力匹配的模型中构建候选。
- [x] 请求级候选排序包含能力、成功率、延迟、额度、冷却和历史证据，并输出可解释分解。
- [x] 网络错误、超时、429、可重试 5xx 和协议错误自动切换候选；401/403 等配置错误按安全策略隔离。
- [x] 避免同一请求重复命中同一模型或失效 Provider，并设置最大尝试次数和总超时预算。
- [x] 非流式与 SSE 流式请求均正确处理首包前失败、半截流和错误事件。

## C. 冷却、恢复与路由后学习

- [x] Provider/模型按失败类型进入分级冷却，持久化并支持人工清除。
- [x] 冷却后通过最小恢复探针重新加入候选池，避免无探测自动恢复。
- [x] 记录候选、尝试链、切换原因、HTTP 状态、错误类型、TTFB、总延迟与最终结果。
- [x] 路由后学习更新成功率与延迟画像，但不能绕过目录接入、能力证据和 Auto 批准。
- [x] 控制台展示调用链、失败切换、冷却恢复和 API Key 使用审计，手机端可完整操作。

## 当前状态

- `deployed`：第三阶段已完成代码实现、标准构建、候选验收、GitHub 合并和 NAS 生产部署。
- 生产镜像 `xing-shu:v0.8.0` healthy，287 models，ready=true；回滚备份已保留。

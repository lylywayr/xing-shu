# 第二阶段：模型发现、能力验证与两级批准

## 验收目标

- [x] 外部 Provider 同步后仅形成待接入目录，未经用户选择不能进入模型池或 Auto。
- [x] 用户选择接入后模型成为 `active`，但未经能力批准不能进入 Auto。
- [x] 批量能力验证覆盖结构化输出、Tools、Vision；结果持久化为可追溯证据。
- [x] 能力治理显示验证覆盖度、失败原因及二级批准动作；支持批量批准、撤销与审计。
- [x] 教师模型仅来自已完成结构化输出验证且已获 Auto 批准的模型。
- [x] FreeLLMAPI 保持独立全量接入和生产路由闸门，不受外部模型二级门禁影响。
- [x] 同步、重启、停用/恢复不会绕过审批或丢失能力证据。

## 当前状态

- `in_progress`：通过全量回归、正式构建与生产迁移验收。

# 第三阶段发现

- 当前 `/api/admin/*` 由 `ADMIN_API_KEY` 或管理员会话保护；该密钥属于管理面，不能暴露给客户端。
- 当前 `/v1/models` 与 `/v1/chat/completions` 没有独立客户端 API Key 管理层。
- 前端没有统一展示 Base URL、接口路径和调用示例，用户只能查看部署配置。
- 第三阶段必须新增独立客户端 Key 注册表和 `/v1/*` 鉴权中间件，而不是显示 `ADMIN_API_KEY`。
- 客户端 Key 应仅创建时返回一次明文，持久化采用哈希；撤销、轮换、作用域和使用审计必须是服务端能力。
- 鉴权启用必须提供兼容迁移模式，避免升级后突然中断存量客户端。
